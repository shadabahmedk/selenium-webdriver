pipeline {
  agent any
  stages {
    stage('Test') {
      steps {
        sh 'python3 GitHubloginTest.py'
      }
    }
}
post { always { findText alsoCheckConsoleOutput: true, refexp: 'There are test failures.', unstableIfFound: true } }
}
